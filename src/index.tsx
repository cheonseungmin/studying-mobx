import React from 'react';
import ReactDOM from 'react-dom';
import {
  FooPage
} from './template';

ReactDOM.render(
  <React.StrictMode>
    <FooPage />
  </React.StrictMode>,
  document.getElementById('root')
);
